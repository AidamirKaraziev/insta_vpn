server {
    listen 80;
    server_name insta-vpn.ru www.insta-vpn.ru;  # Замените на ваш домен

    location / {
        proxy_pass http://web:80;  # Ваш сервис web (Nginx) в Docker Compose
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
